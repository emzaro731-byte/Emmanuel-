name: destiny_ai
description: Destiny AI mobile app built with Flutter and Supabase.
publish_to: "none"
version: 3.0.0+30

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.10.3
  http: ^1.5.0
  image_picker: ^1.2.0
  shared_preferences: ^2.5.3
  url_launcher: ^6.3.2
  flutter_tts: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
