name: Destiny AI Build

on:
push:
branches:
- main
workflow_dispatch:

jobs:
build:
runs-on: ubuntu-latest

steps:
  - name: Start
    run: echo "Destiny AI workflow started successfully"

  - name: Checkout
    uses: actions/checkout@v4

  - name: Show files
    run: |
      echo "Repository:"
      pwd
      echo "Files:"
      ls -la